public static bool linearSearch(int[] Vector, int Number)
        {
            for (int i = 0; i < Vector.Length; i++) { if (Vector[i] == Number) return true; }          
            return false;
        }